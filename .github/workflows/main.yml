name: Build
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - setup-java: {java-version: '21', distribution: 'temurin'}
      - run: mvn package
      - uses: actions/upload-artifact@v4
        with: {name: plugin, path: target/*.jar}
